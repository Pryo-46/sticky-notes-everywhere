/**
 * アプリケーション全体で使用するUI定数
 */

// ========================================
// サイズ制限
// ========================================

/** 付箋の最小幅（px） */
export const MIN_STICKY_WIDTH = 150;

/** 付箋の最小高さ（px） */
export const MIN_STICKY_HEIGHT = 100;

// ========================================
// ドラッグ関連
// ========================================

/** 画面端での最小表示領域（付箋の一部が必ず見えるように）（px） */
export const MIN_VISIBLE_AREA = 50;

/** ドラッグ検出のしきい値（px） - この距離以上動いたらドラッグと判定 */
export const DRAG_THRESHOLD = 5;

// ========================================
// メニューバー
// ========================================

/** メニューバーのボタン角丸（px） */
export const BUTTON_RADIUS = 4;

/** メニューバーのボタン間隔（px） */
export const BUTTON_GAP = 8;

// ========================================
// z-index
// ========================================

/** z-indexの安全な最大値 */
export const MAX_SAFE_ZINDEX = 2147483600;

/** ドラッグプレビューのz-index（最前面） */
export const DRAG_PREVIEW_ZINDEX = 2147483647;
